<h1>MY FAVORITE MUSIC ALBUMS</h1>

<p>
  Powered by Spring Boot 3.0
</p>

<h3>User Profile</h3>
<ul>
    <li>Username: {{authentication.name}}</li>
    <li>Authorities: {{authentication.authorities}}</li>
</ul>

<form action="/logout" method="post">
    <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
    <button type="submit">Logout</button>
</form>

<hr>



<hr />
ADD new:
<form action="/new-album" method="post">
  Artist<input type="text" name="artist">
    Title<input type="text" name="title">
      Year<input type="text" name="yearOfRelease">
            Genre<input type="text" name="genre">
                    <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
  <button type="submit">Submit</button>
</form>


<hr />
SEARCH:

<form action="/search" method="post">
    <label for="value">Search:</label>
    <input type="text" name="value">
    <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
    <button type="submit">Search</button>
</form>

{{#search}}
    Search by Title, Artist, etc . (not working for year!): {{value}}
{{/search}}


<ul>
  {{#albums}}
    <li>{{artist}} - {{title}} - {{yearOfRelease}} - {{genre}}  
    
              <form action="/delete/albums/{{id}}" method="post">
                <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}">
                <button type="submit">Delete</button>
            </form>
            </li>
  {{/albums}}
</ul>